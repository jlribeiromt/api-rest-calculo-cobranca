# Please refer https://aka.ms/HTTPSinContainer on how to setup an https developer certificate for your ASP .NET Core service.

version: '3.6'

services:
  calculocobrancataxajuroapi:
    image: calculocobrancataxajuroapi
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "64978:80"
